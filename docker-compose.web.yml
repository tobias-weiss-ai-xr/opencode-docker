# OpenCode Web Interface
# Usage: docker compose -f docker-compose.yml -f docker-compose.web.yml up

services:
  opencode:
    container_name: opencode-web
    ports:
      - "3000:3000"
    command: ["opencode", "web"]