# OpenCode Headless Server
# Usage: docker compose -f docker-compose.yml -f docker-compose.serve.yml up

services:
  opencode:
    container_name: opencode-serve
    ports:
      - "3000:3000"
    command: ["opencode", "serve", "--hostname", "0.0.0.0"]